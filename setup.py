from setuptools import setup, find_packages

setup(
    name="vdraw",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # List dependencies here (e.g., "requests>=2.0.0")
    ],
)
